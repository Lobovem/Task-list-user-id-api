export interface IUsers {
  id: string;
  name: string;
  username: string;
  email: string;
}

export interface IAlbums {
  id: string;
  title: string;
}

export interface IPhotos {
  id: string;
  title: string;
  url: string;
  thumbnailUrl: string;
}
